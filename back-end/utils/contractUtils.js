// utils/contractUtils.js

const generateContractSummary = (contractText) => {
    // Simple example: return first 2 lines as summary
    const maxLength = 300;
    return text.length > maxLength ? text.slice(0, maxLength) + '...' : text;
  };
  
  module.exports = {
    generateContractSummary,
  };
  